version: '3.9'

services:
  adminer:
    image: adminer:latest
    environment:
      - ADMINER_DEFAULT_SERVER=postgres
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network
    ports:
      - "${ADMINER_PORT:-8080}:8080"

  postgres:
    image: postgres:18-alpine
    container_name: demo-postgres
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/18/docker
    volumes:
      - pg-data:/var/lib/postgresql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped

volumes:
  pg-data:

networks:
  app-network:
    driver: bridge


#  stats-service:
#    build:
#      context:
#      dockerfile:
#    container_name:
#    ports:
#    depends_on:
#      -
#services:
#  web:
#    image: nginx:alpine
#    ports:
#      - '8080:80'
#
#  frontend:
#    networks:
#      - frontend-network
#
#  backend:
#    command:
#      - npm start
#    depends_on: # Attend que postgres soit initialisé, si jamais ce service backend a besoin de la db
#      - postgres
#    networks:
#      - frontend-network
#      - backend-network
#

#    volumes:
#      - pg-data:/var/lib/postgresql
#
#    networks:
#      - backend-network
#
#    env_file: # ne pas hésiter à rename les .env, les vars dans environment: sont prioritaire
#      - .env
#      - .env.local
